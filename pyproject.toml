[tool.poetry]
name = "querylifter"
version = "0.1.0"
description = ""
authors = ["reivaj <reivaj2dev@gmail.com>"]
readme = "README.md"
packages = [
    { include = "cli", from = "src" }
]

[tool.poetry.dependencies]
python = ">=3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
querylifter = "cli.app:__main__"